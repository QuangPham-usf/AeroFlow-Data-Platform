# dbt-core pinned to the 1.10 line: dbt_project.yml relies on 1.10-only
# behavior (require_generic_test_arguments_property flag, `arguments:` test
# nesting). dbt-snowflake 1.9.x is compatible with core 1.10 (adapters are
# decoupled from core since 1.8).
dbt-core==1.10.5
dbt-snowflake==1.9.2
sqlfluff==3.4.0
# setup.cfg uses the dbt templater -- without this package `sqlfluff lint`
# cannot render models (and the lint job is blocking).
sqlfluff-templater-dbt==3.4.0
